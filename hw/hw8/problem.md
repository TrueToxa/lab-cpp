Требуется реализовать функции чтения и создания односвязного списка Node* read_list(Node* head) и разворота односвязного списка Node* reverse(Node* head), где Node это

struct Node {
  int key;
  Node* next;
};
Функция разворачивает список за линейное время от длины списка 
O(n)
O(n) и возвращает указатель на узел, являющийся новым началом.

Заполнение первоначального списка осуществляется из консоли и должно быть реализовано в функции Node* read_list(Node*& head) через вызов функции push_back из контеста с односвязным списком (её реализацию тоже надо добавить в посылку). Список заканчивается 0.

Гарантируется, что проверяющий код вызывает ваши функции следующим образом:

